>change the query result to 
if ($result && mysqli_num_rows($result)>0) {
    return $result;
}
else{

    return false;
}

>add else to every list page to show table is empty

after accept the borrow request reduce the quantity and also disable borrow button for under quantity.


Admin login:
mail: t@mail.com
pass: 2233

you can register student and teacher and approved them from admin panel.